[build]
  publish = "analysis/report/_book"
  command = "quarto render analysis/report --to html"

[build.environment]
  QUARTO_VERSION = "1.5.57"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
